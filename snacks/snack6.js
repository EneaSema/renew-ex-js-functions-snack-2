// 🏆 Snack 6
// Crea un contatore automatico con setInterval
// Definisci una funzione creaContatoreAutomatico che accetta un intervallo di tempo e restituisce una funzione che avvia un setInterval, incrementando un contatore e stampandolo.

// console.log("Snack 6");
// const t = 1000;

// function creaContatoreAutomatico(t) {
//   let counter = 0;
//   console.log(`Il contatore parte da: ${counter}`);

//   const startCounter = setInterval(() => {
//     counter++;
//     console.log(`Aumento del contatore:${counter}`);
//   }, t);
//   return startCounter;
// }

// const startContatore = creaContatoreAutomatico(t);

// setTimeout(() => {
//   clearInterval(startContatore);
//   console.log(`Fine contatore`);
// }, 5000);
